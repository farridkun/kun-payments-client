import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['local-origin.dev', '*.local-origin.dev', '*', 'https://kun-sgd-1-u.run.place', 'http://159.89.204.161:3000'],
};

export default nextConfig;
